server:
  port: 8091

spring:
  application:
    name: reports-service
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}
  datasource:
    url: ${REPORTS_DB_URL:jdbc:postgresql://localhost:5432/ticketwave_aw_report}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  sql:
    init:
      mode: always
      continue-on-error: true

ticketwave:
  jwt:
    secret: ${JWT_SECRET:ChangeThisSecretKeyInProductionToASecureRandomValueOfAtLeast32BytesLong}
    expiration-ms: ${JWT_EXPIRATION:86400000}

springdoc:
  api-docs:
    path: /v3/api-docs/reports
  swagger-ui:
    path: /reports/swagger-ui.html

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

logging:
  level:
    com.ticketwave.reports: DEBUG


# http://localhost:8091/reports/swagger-ui/index.html

# http://localhost:9000/ticketorder/swagger-ui/index.html
# http://localhost:9000/reports/swagger-ui/index.html
# http://localhost:9000/legacy/swagger-ui/index.html
